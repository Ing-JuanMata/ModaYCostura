name: deploy
run-name: deploying api
on:
    push:
        branches: [main]
    workflow_dispatch:
    pull_request:
        types: [closed]
        branches: [main]
jobs:
    build-container:
        runs-on: self-hosted
        steps:
            - uses: actions/checkout@v4
            - name: build
              run: docker build -t ghcr.io/ing-juanmata/modaycostura:latest .
            - name: login to docker
              run: echo ${{ secrets.DOCKER_KEY }} | docker login --username ${{github.actor}} --password-stdin ghcr.io
            - name: push image
              run: docker push ghcr.io/ing-juanmata/modaycostura:latest
            - name: logout docker
              run: docker logout
            - name: remove container
              run: docker rm -f myc
            - name: create container in port 80
              run: docker run --name myc --network=composers_default -d ghcr.io/ing-juanmata/modaycostura:latest
